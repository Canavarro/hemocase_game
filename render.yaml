services:
  - type: web
    name: hemocase-scape
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start -w @hemocase/server
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "22"
