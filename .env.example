# Banco de dados (Neon · Lakebase Postgres)
# Projeto: LAGEM (rapid-glitter-95673289) · Organização: LAGEM (org-late-hall-61778496)
# Preencha com `neon env pull` (CLI autenticado) ou copie do console do Neon.
# Sem DATABASE_URL o jogo roda 100% em memória, como sempre (rankings ficam indisponíveis).
DATABASE_URL=

# Chave da API do Neon (opcional; usada pelo CLI/agentes, nunca pelo servidor do jogo)
# NEON_API_KEY=
